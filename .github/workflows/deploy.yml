name: Build and Deploy to Dev

on:
  push:
    branches:
      - dev

env:
  ACR_NAME: alattar
  ACR_LOGIN_SERVER: alattar-hygkehhmfvekb0fy.azurecr.io
  CONTAINER_APP_NAME: ca-cloudmediahub-api
  RESOURCE_GROUP: rg-cloudmediahub-dev

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Log in to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Log in to ACR
        run: az acr login --name $ACR_NAME

      - name: Build and push image
        run: |
          IMAGE_TAG=${{ github.sha }}
          docker build -t $ACR_LOGIN_SERVER/cloudmediahub-api:$IMAGE_TAG .
          docker push $ACR_LOGIN_SERVER/cloudmediahub-api:$IMAGE_TAG
          echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV

      - name: Deploy to Dev
        run: |
          az containerapp update \
            --name $CONTAINER_APP_NAME \
            --resource-group $RESOURCE_GROUP \
            --image $ACR_LOGIN_SERVER/cloudmediahub-api:$IMAGE_TAG